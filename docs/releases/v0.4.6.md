## rgctl v0.4.6

> **Note:** v0.4.6 release archives shipped the CLI as **`rg-build`**. Current docs and main use **`rgctl`** ([v0.4.7](v0.4.7.md)).

This release lands the **refactor** branch: faster CFG and semantic indexing, a first-class **agent skill installer**, dashboard export work, and a new set of **step-by-step guides** on the website (CoolStore walkthroughs for every major CLI feature).

## Guides on the website

Start here — practical how-tos with a shared CoolStore (`konveyor-ecosystem/coolstore`) example:

**https://shaaf.dev/rgctl/docs/guides/**

| Guide | Command |
|-------|---------|
| [Discovering and Indexing](https://shaaf.dev/rgctl/docs/guides/discovering-and-indexing/) | `discover` |
| [Graph Query Language](https://shaaf.dev/rgctl/docs/guides/graph-query-language/) | `gql` |
| [Blast Radius Analysis](https://shaaf.dev/rgctl/docs/guides/blast-radius-analysis/) | `blast-radius` |
| [Semantic Search](https://shaaf.dev/rgctl/docs/guides/semantic-search/) | `semantic` |
| [Graph Metrics](https://shaaf.dev/rgctl/docs/guides/graph-metrics/) | `metrics` |
| [Community Detection](https://shaaf.dev/rgctl/docs/guides/community-detection/) | `communities` |
| [Program Slicing](https://shaaf.dev/rgctl/docs/guides/program-slicing/) | `slice` |
| [Hybrid CPG](https://shaaf.dev/rgctl/docs/guides/hybrid-cpg/) | `cpg` |
| [Inspecting CFG, PDG, and Dominance](https://shaaf.dev/rgctl/docs/guides/inspecting-cfg-pdg-dominance/) | `inspect` |
| [Exporting Graphs](https://shaaf.dev/rgctl/docs/guides/exporting-graphs/) | `export` |
| [CI Policy Checks](https://shaaf.dev/rgctl/docs/guides/ci-policy-checks/) | `check` |
| [HTTP Server and Dashboard](https://shaaf.dev/rgctl/docs/guides/http-server-and-dashboard/) | `serve` |
| [Migration Planning](https://shaaf.dev/rgctl/docs/guides/migration-planning/) | `discover --export-migration-hints` |
| [Agent Skill](https://shaaf.dev/rgctl/docs/guides/agent-skill/) | `install --skill` |

Also: [User Guide](https://shaaf.dev/rgctl/docs/user-guide/) · [JSON API](https://shaaf.dev/rgctl/docs/json-api/) · [Docs hub](https://shaaf.dev/rgctl/docs/)

## Highlights

### Agent skill installer

`rgctl install --skill` copies the bundled `skills/rgctl/` tree (embedded in the binary at compile time) into the target repo:

- `<repo>/.claude/skills/rgctl/`
- `<repo>/.cursor/skills/rgctl/`

Default `--host all`; `--host claude` or `--host cursor` to limit. Identical files are left unchanged; differing files require `--force`. `-f json` reports each write (`created` / `unchanged` / `overwritten` / `skipped_exists`).

```bash
rgctl install --skill
rgctl -r /path/to/repo -f json install --skill
```

No git checkout of rgctl is required — a GitHub Release binary is enough.

### Semantic search: vocab by default, parallel index

- `rgctl semantic index` uses the compiled **vocab** embedder unless you pass `--embedder code-daemon` or `--embedder hash`.
- Index build is parallel on CPU/I/O-bound stages (batched embed, body reads, ONNX `session.run` with batch 32 and fallback). The on-disk index is still one bincode write at the end.
- Distill / Hamming fusion path is aligned with the vocab matrix (v2).

### Faster `--with-cfg` discover

- **One parse per file:** CFG work is grouped by source file. Each Rayon task parses that file once, then builds every function’s CFG from the same tree. Cache hits still skip parse.
- **Faster dominators:** reverse postorder + dense Cooper–Harvey–Kennedy `Vec<u32>` idoms. Public `DominatorTree` maps stay `HashMap<BlockId, …>`.
- **No CFG/PDG deep clone into the archive:** `FunctionAnalysis` / `CfgPdgRecord` share `Arc<ControlFlowGraph>` and `Arc<ProgramDependenceGraph>`. Wire format is unchanged (serde `rc`).

Linux kernel **cold** `discover` (default flags, no CFG) remains inside the 170s +10% gate on the maintainer machine (~146s wall, ~2.66M nodes).

### Dashboard export

Streaming/export helpers for analysis, blast, dataflow, function metrics, taint, and source catalog — less overhead when building the optional dashboard bundle.

### Contributor checklist

[docs/contributor-checklist.md](https://shaaf.dev/rgctl/docs/contributor-checklist/) — language, feature, and CI-gate hub (fixes #62, PR #64).

## CLI

| Change | Detail |
|--------|--------|
| `install --skill` | New subcommand; `--host all\|claude\|cursor`; `--force` |
| `-f json install` | `schema_version` 1 — see [JSON API § install](https://shaaf.dev/rgctl/docs/json-api/) |
| `semantic index` | Default embedder **vocab** |

## Docs and site

- New `docs/guides/` tree (14 guides) mirrored on the site at `/docs/guides/`.
- Guides use **CoolStore** from [konveyor-ecosystem/coolstore](https://github.com/konveyor-ecosystem/coolstore).
- User Guide §1 documents `install --skill`; JSON API documents the payload.

## Merged PRs

- #65: Refactor — CFG/semantic performance, skills installer, guides, dashboard export.
- #64: Contributor checklist hub (@savitharaghunathan).

## Commit range

Compare: `v0.4.5...v0.4.6`

Notable commits:

- `f706150` Default semantic index embedder is vocab (opt in to code-daemon / hash).
- `ac9826e` Vocab matrix / code-daemon distill path.
- `ba4a6e9` Parallel semantic index build (embed, bodies, batched ONNX).
- `4014bd0` CFG: one parse per file, faster dominators, Arc-shared CFG/PDG.
- `be94837` Dashboard export optimization.
- `18b7bf2` `rgctl install --skill` (shipped as `rg-build` in v0.4.6 binaries).
- `f51b7b8` / `72c52c8` Feature guides + agent skill guide.
- `2b0e988` Contributor checklist (fixes #62).

## Thanks

Thanks to @savitharaghunathan for the contributor checklist.

## Install

Download platform archives from this GitHub Release, put the CLI on your `PATH` (v0.4.6 archive: **`rg-build`**; current: **`rgctl`**), then:

```bash
rgctl --version
rgctl install --skill
```

See [Install](https://shaaf.dev/rgctl/install/) and [User Guide §1](https://shaaf.dev/rgctl/docs/user-guide/).
