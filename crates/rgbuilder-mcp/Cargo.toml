[package]
name = "rgbuilder-mcp"
version = "0.4.6"
edition.workspace = true
rust-version.workspace = true
description = "MCP stdio server for rgBuilder (no HTTP, no discover pipeline)"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = "1"
rgbuilder-service = { workspace = true }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
